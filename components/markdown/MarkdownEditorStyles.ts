const markdownWrapper = {
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
};

const textArea = {
  width: '100%',
  flexGrow: 1,
  color: 'var(--secondary-font-color)',
  marginBottom: '20px',
  border: 'none',
  outline: 'none',
  appearance: 'none',
  background: 'none',
  resize: 'none',
};

export const styles = {
  markdownWrapper,
  textArea,
};
