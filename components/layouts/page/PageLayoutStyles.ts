const box = {
  flexGrow: 1,
};

const container = {
  paddingTop: '24px',
  paddingBottom: '45px',
  minHeight: '50vh',
};

const paper = {
  backgroundColor: 'var(--cards-color)',
  color: 'var(--secondary-font-color)',
  padding: '24px',
  minWidth: '100%',
};

export const styles = {
  box,
  container,
  paper,
};
